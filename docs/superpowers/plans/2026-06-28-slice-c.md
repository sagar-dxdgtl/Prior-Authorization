# Slice C — NPI→TIN crosswalk ingestion (Phase 3)

**Context:** `domain/tin_crosswalk.TinCrosswalk` already loads an NPI→TIN file (CSV `npi,tin[,payer]`
/ JSON) and feeds `TinScopeSource`; the documented blocker was always "needs a data file." Slice C
builds the **ingestion pipeline** that produces that file from a payer **Transparency-in-Coverage (TiC)
in-network MRF**, tested against synthetic data so it's ready for real data.

**Scope (this pass):**
- **C1 — TiC streaming ingester** (`domain/tic_ingest.py` + `scripts/ingest_tic.py`): `ijson`-stream a
  TiC in-network file (`.json` / `.json.gz`), extract every `provider_groups[]` (`npi: [...]` +
  `tin: {type, value}`) from both `provider_references[]` and `in_network[].negotiated_rates[]`, and
  write `npi,tin,payer` CSV. Optional NPI allow-list filter (so we can slice one plan's NPIs out of a
  huge file). Constant-memory streaming — never loads the whole MRF.
- **C2 — Integration**: the produced CSV loads via `TinCrosswalk(path=...)` and flows through
  `TinScopeSource` (contradiction when the billing TIN isn't among the in-network TINs). Add a test.
- Add `ijson` dependency.

**C3 — TIN filter** (`domain/tic_ingest.py` + `scripts/ingest_tic.py`): providers appear in a payer's
TiC under their **practice EIN (TIN)**, often paired with *individual* rendering NPIs rather than the
group NPI.  A `tin_filter` parameter (set of TINs) was added to `ingest_tic` so callers can extract
only the provider_groups belonging to a specific practice by EIN, and thus discover the individual NPIs
under that TIN.  Non-digit characters (e.g. dashes in `93-3510922`) are stripped before comparison.
Both `npi_filter` and `tin_filter` compose as an intersection when both are supplied.  The CLI gains
`--tin-file` (one TIN per line, `#`-comment lines ignored) and repeatable `--tin` flags.

**Deferred (documented):** running against real multi-GB/TB TiC files (needs the file + disk; the
ingester is ready); empirically-verified **additional payer FHIR/directory adapters** (needs live
endpoint discovery — not done against real payers in this autonomous run).

**Tests:** synthetic TiC fixture (both `provider_references` and embedded shapes) → ingest → expected
`(npi,tin)` rows; NPI-filter; `.json.gz` variant; end-to-end TinCrosswalk→TinScopeSource. Pure/offline
(no network, no PHI). Keep `pytest -m "not live and not db"` + `-m db` green; ruff clean.
